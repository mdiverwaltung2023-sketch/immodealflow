DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"
PORT=4000
FRONTEND_ORIGIN="http://localhost:3000"
ANTHROPIC_API_KEY=""
# Clerk Secret Key (sk_test_... / sk_live_...) — Pflicht fuer Login + alle
# auth-geschuetzten Routen. Wert aus dem Clerk-Dashboard oder den Railway-
# Variablen. Ohne diesen Key blockt requireAuth lokal alles mit 500/401.
CLERK_SECRET_KEY=""

