// Phase L5.3 — alte Anlage-Form ersetzt durch generische
// `<RentalUnitForm mode="create" />` aus @/components/RentalUnitForm.
// Re-Export, damit alte Imports nicht brechen.
export { RentalUnitForm as NewRentalUnitForm } from "@/components/RentalUnitForm";
