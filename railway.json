{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate --schema=backend/prisma/schema.prisma && npm run build -w backend"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=backend/prisma/schema.prisma && npm run start -w backend",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
